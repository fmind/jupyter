{
    // Terminal
    // @jupyterlab/terminal-extension:plugin
    // Terminal settings.
    // *************************************

    // Theme
    // The theme for the terminal.
    "theme": "dark",
        
    // Treat option as meta key on macOS
    // Option key on macOS can be used as meta key.
    "macOptionIsMeta": true
}
